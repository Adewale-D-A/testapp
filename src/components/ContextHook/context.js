import { createContext } from "react";

export const context1 = createContext({});
export const context2 = createContext({});
export const context3 = createContext({});

export const overallState = createContext({});
